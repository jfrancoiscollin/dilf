# Sentinel file. Touch and commit to trigger
# .github/workflows/regenerate-prose-embeddings.yml. The file's content
# doesn't matter — only the path-filtered push event matters.
#
# Last triggered: 2026-05-26 — initial run via CI from the sandbox
# (huggingface.co blocked there, so GitHub Actions does the dense
# embedding pass).
